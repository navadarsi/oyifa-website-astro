# Sanity CMS configuration
# Project ID is public (it ships in client bundles). The token is optional and
# only needed to read draft/unpublished content.
SANITY_PROJECT_ID=zxslyc4b
SANITY_DATASET=production
SANITY_API_VERSION=2024-01-01
SANITY_API_TOKEN=
